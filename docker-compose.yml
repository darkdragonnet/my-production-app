version: '3.8'

services:
  flask-api:
    build: .
    container_name: flask-production
    restart: always
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - PYTHONUNBUFFERED=1
